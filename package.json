{
  "name": "CLP",
  "version": "0.1.0",
  "description": "中电互动展屏",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . --platform=win32 --arch=x64 --overwrite --asar --out=bin --ignore=node_modules  --ignore=.gitignore --ignore=.project"
  },
  "devDependencies": {
    "electron-prebuilt": "^1.2.0",
    "electron-packager": "~7.7.0"
  }
}